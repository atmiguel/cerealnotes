{{ define "title" }}Home{{ end }}

{{ define "js" }}<script src="/static/js/home.js"></script>{{ end }}

{{ define "content" }}
    <div class="mui-container">
        <h1 class="mui--text-center">
            CerealNotes
        </h1>

        Welcome user: {{ . }}

        <br />
        <button id="logout-button" type="button" class="mui-btn mui-btn--primary">Logout</button>
    </div>
{{ end }}